@baseUrl = https://cst8916-a1-cuaebjghbuf4cybz.westus3-01.azurewebsites.net

### 1) GET all tasks (200)
GET {{baseUrl}}/tasks

### 2) GET a single task (200)
GET {{baseUrl}}/tasks/1

### 3) GET a task that doesn't exist (404)
GET {{baseUrl}}/tasks/9999

### 4) POST a new task (valid data) (201)
POST {{baseUrl}}/tasks
Content-Type: application/json

{
  "title": "Complete Assignment 1",
  "description": "Extend the REST API with tasks",
  "user_id": 1,
  "completed": false
}

### 5) POST a task with missing title (400)
POST {{baseUrl}}/tasks
Content-Type: application/json

{
  "description": "Missing title field",
  "user_id": 1,
  "completed": false
}

### 6) POST a task with invalid user_id (400)
POST {{baseUrl}}/tasks
Content-Type: application/json

{
  "title": "Invalid user test",
  "description": "user_id does not exist",
  "user_id": 9999,
  "completed": false
}

### 7) PUT to update a task (200)
PUT {{baseUrl}}/tasks/2
Content-Type: application/json

{
  "title": "Build API (updated)",
  "completed": true
}

### 8) DELETE a task (204)
# For a predictable demo, restart the App Service before recording so the POST in step 4 creates id=3.
DELETE {{baseUrl}}/tasks/3

### 9) GET tasks for a specific user (200)
GET {{baseUrl}}/users/1/tasks